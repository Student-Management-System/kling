import * as fromFileTab from "../file-tab.actions";

describe("loadFileTabs", () => {
	it("should return an action", () => {
		//expect(fromFileTab.addFileTab().type).toBe("[FileTab] Load FileTabs");
	});
});
